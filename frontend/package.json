{
  "name": "newspulse-frontend",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "api": "cd .. && python3 -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8000",
    "dev:all": "bash ../scripts/dev.sh",
    "build": "vite build",
    "preview": "vite preview",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@vitejs/plugin-react": "^4.5.2",
    "vite": "^6.3.5"
  }
}
